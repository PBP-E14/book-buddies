name: Deploy to Fly.io

on:
    push:
        branches:
            - main

jobs:
    Deploy:
        runs-on: ubuntu-latest
        steps:
            - name: Cloning repo
              uses: actions/checkout@v4

            - name: Deploy to Fly
              run: fly deploy
